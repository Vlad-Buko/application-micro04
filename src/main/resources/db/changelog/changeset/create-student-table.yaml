databaseChangeLog:
  - changeSet:
      id: create-students
      author: Vladislav
      changes:
        - createTable:
            tableName: student
            columns:
              - column:
                  name: id
                  type: integer
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: true
              - column:
                  name: last_name
                  type: varchar(255)
                  constraints:
                    nullable: true
              - column:
                  name: team_id
                  type: integer
                  constraints:
                    nullable: false
                    foreignKeyName: fk_abc_account
                    references: team(id)
              - column:
                  name: score
                  type: integer